services:
  nginx:
    image: nginx:latest
    container_name: Nginx-Acestream
    restart: unless-stopped
    ports:
      - "4002:4002"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - acestream-network

networks:
  acestream-network:
    external: true

